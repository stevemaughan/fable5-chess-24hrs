# Eval symmetry check: eval(fen) from white must equal eval(mirrored fen) from black.
# Uses "go depth 1" score as proxy is unreliable; instead we add an "eval" UCI command later.
# Placeholder — see evaltest in engine.
